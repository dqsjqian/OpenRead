## E5: 把 third_party 标为 vendored + 按二进制处理，
## 避免 GitHub 把第三方代码统计进仓库语言分布，并加速 git diff/blame。

# 整个 third_party 目录按 vendored 对待（linguist 会在 GitHub 语言统计里忽略）
* linguist-vendored
* linguist-generated=false

# 大型源文件按二进制差异处理（diff 显示 "Binary files differ"，不再逐行比较）
# 这些文件平时不需要人工 review 差异
sqlite3_src/sqlite3.c     binary
sqlite3_src/sqlite3.h     binary
openssl/**                binary
curl/**                   binary

# QuickJS：保留文本 diff 但忽略语言统计
quickjs/*.c               -diff
quickjs/*.h               -diff

# Gumbo / json / pybind11：正常 diff 但不计入语言统计（由上面的 `*` 规则已处理）
